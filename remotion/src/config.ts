// Flip these as you add real assets.

// true  → render animated placeholders (works with zero recordings).
// false → use your Screen Studio clips from public/sceneNN.mp4.
export const USE_PLACEHOLDERS = true;

// true once you've dropped a voiceover at public/vo.m4a (or .mp3).
export const HAS_VO = false;
export const VO_FILE = "vo.m4a";

// Optional: burn the VO lines in as subtitles (handy for silent autoplay).
export const SHOW_SUBTITLES = false;
